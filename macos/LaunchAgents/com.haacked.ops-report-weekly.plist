<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.haacked.ops-report-weekly</string>

    <!--
        Use zsh login shell so .zshenv provides PATH (Homebrew, ~/.local/bin,
        Cargo) and path_helper picks up entries from /etc/paths.d.
    -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/zsh</string>
        <string>-lc</string>
        <string><![CDATA[
log_dir="$HOME/.local/state/ops-report-weekly"
mkdir -p "$log_dir"
exec "$HOME/.dotfiles/bin/ops-report-run" week >> "$log_dir/launchd.log" 2>&1
]]></string>
    </array>

    <!--
        Fire Monday at 09:00 local time. The weekly digest leads with the full
        metric tables; the Tue-Fri daily reports (com.haacked.ops-report-daily)
        are lean exception reports. StartCalendarInterval uses the system's local
        time zone; Weekday 1=Monday.
    -->
    <key>StartCalendarInterval</key>
    <array>
        <dict>
            <key>Weekday</key>
            <integer>1</integer>
            <key>Hour</key>
            <integer>9</integer>
            <key>Minute</key>
            <integer>0</integer>
        </dict>
    </array>
</dict>
</plist>
